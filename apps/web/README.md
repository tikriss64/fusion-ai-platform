# apps/web — vaciadodepisos.cat

Fachada comercial y fuente de leads. Web en producción (HTML estático + PHP,
multiidioma). Su formulario de contacto inserta directamente en la tabla
`leads` de Supabase (clave anon), alimentando al CRM y al Inbox.

**Origen:** se referenciará desde `PROYECTO VACIADODEPISOS` (solo lectura).

No requiere reescritura: solo se integra a través del formulario y el blog.

> Carpeta vacía por ahora.
